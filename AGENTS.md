# bin 网关维护 SOP

- 这是当前设备唯一使用和维护的网关仓库。
- 不要修改、构建、运行、部署或重启同级目录中的 `ljt/`、`ljt1/`。它们只可作为历史资料只读查看；只有用户在当前对话中明确授权时例外。
- 固件输出出现 `LJT:` 日志标签不等于设备切回 ljt 网关，不要仅凭该标签改变服务端目标。
- 所有服务端的打断、AEC、ASR、TTS、阈值、Docker、CI/CD 和生产部署改动都应落在本仓库。
- 每次修改前后检查本仓库自己的 `git status`；有效实验先提交并推送形成回退点，再继续下一轮。
- 部署必须对应明确的 Git 提交。不得提交 `.env`，不得用空配置覆盖服务器 `/home/admin/bin-gateway/.env`。
- 部署后检查健康状态和运行日志，并保留上一版本以便回滚。
